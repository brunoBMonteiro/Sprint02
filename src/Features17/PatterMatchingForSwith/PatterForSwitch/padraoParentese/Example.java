package Features17.PatterMatchingForSwith.PatterForSwitch.padraoParentese;

// Não suporta por que uso o java 11

public class Example {
    public static void main(String[] args) {
        /*
        static double getDoubleValueUsingParenthesizedPatterns(Object o) {
    return switch (o) {
        case String s && s.length() > 0 && !(s.contains("#") || s.contains("@")) -> Double.parseDouble(s);
        default -> 0d;
    };
}
         */
    }
}
